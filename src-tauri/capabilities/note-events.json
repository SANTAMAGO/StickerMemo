{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "note-events",
  "description": "Allow Floating Note windows to receive locale-changed broadcasts.",
  "windows": [
    "note-*"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
