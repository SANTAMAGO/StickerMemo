<Window x:Class="StickerMemo.Views.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="StickerMemo Tray Menu"
        Width="220" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Deactivated="OnWindowDeactivated"
        KeyDown="OnWindowKeyDown">

    <Window.Resources>
        <!-- Custom Paper Menu Item Style -->
        <Style x:Key="TrayMenuItemButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#15000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Destructive / Quit Button Style -->
        <Style x:Key="TrayMenuQuitButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#20EF4444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#35EF4444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer Shadow Margin Grid -->
    <Grid Margin="12">
        <Border Background="#FAFAF7" CornerRadius="10" BorderBrush="#35000000" BorderThickness="1" Padding="6">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Direction="270" Opacity="0.22"/>
            </Border.Effect>

            <StackPanel Orientation="Vertical">
                <!-- App Mini Header -->
                <DockPanel LastChildFill="True" Margin="8,4,8,8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📌" FontSize="11" Margin="0,0,5,0"/>
                        <TextBlock Text="StickerMemo" FontSize="11.5" FontWeight="Bold" Foreground="#374151"/>
                    </StackPanel>
                    <TextBlock Text="v1.0" FontSize="9.5" Foreground="#9CA3AF" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>

                <Separator Background="#20000000" Margin="4,0,4,4"/>

                <!-- 1. New Note -->
                <Button Style="{StaticResource TrayMenuItemButtonStyle}" Click="OnNewNoteClick">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="➕" FontSize="11" Width="20" VerticalAlignment="Center"/>
                        <TextBlock Text="New Note" FontSize="11.5" FontWeight="Medium" Foreground="#1F2937" VerticalAlignment="Center"/>
                    </DockPanel>
                </Button>

                <!-- 2. Show / Hide Deck Toggle -->
                <Button x:Name="ToggleDeckBtn" Style="{StaticResource TrayMenuItemButtonStyle}" Click="OnToggleDeckClick">
                    <DockPanel LastChildFill="True">
                        <TextBlock x:Name="ToggleDeckIcon" Text="🗂️" FontSize="11" Width="20" VerticalAlignment="Center"/>
                        <TextBlock x:Name="ToggleDeckText" Text="Show Deck" FontSize="11.5" FontWeight="Medium" Foreground="#1F2937" VerticalAlignment="Center"/>
                    </DockPanel>
                </Button>

                <!-- 3. Start with Windows Toggle -->
                <Button Style="{StaticResource TrayMenuItemButtonStyle}" Click="OnStartWithWindowsClick">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="⚙️" FontSize="11" Width="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Start with Windows" FontSize="11.5" FontWeight="Medium" Foreground="#1F2937" VerticalAlignment="Center"/>
                        <Border x:Name="StartupCheckBadge" DockPanel.Dock="Right" HorizontalAlignment="Right" Background="#2010B981" CornerRadius="4" Padding="5,1" Margin="4,0,0,0">
                            <TextBlock x:Name="StartupCheckText" Text="✓ ON" FontSize="9" FontWeight="Bold" Foreground="#059669"/>
                        </Border>
                    </DockPanel>
                </Button>

                <!-- 4. Import Sticky Notes -->
                <Button Style="{StaticResource TrayMenuItemButtonStyle}" Click="OnImportStickyNotesClick">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="📥" FontSize="11" Width="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Import Sticky Notes" FontSize="11.5" FontWeight="Medium" Foreground="#1F2937" VerticalAlignment="Center"/>
                    </DockPanel>
                </Button>

                <Separator Background="#20000000" Margin="4,4"/>

                <!-- 5. Quit App -->
                <Button Style="{StaticResource TrayMenuQuitButtonStyle}" Click="OnQuitAppClick">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="🚪" FontSize="11" Width="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Quit App" FontSize="11.5" FontWeight="SemiBold" Foreground="#DC2626" VerticalAlignment="Center"/>
                    </DockPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
